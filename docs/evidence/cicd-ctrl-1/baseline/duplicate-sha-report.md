# CICD-CTRL-1 — Baseline Duplicate-SHA Report

Derived from `ci-runs.json` (read-only GitHub Actions run history). Runner-minute billing is not exposed by the runs API; see `runtime-summary.md` (APPROXIMATE FROM RUN DURATION).

- Total runs analysed: **17**
- Distinct head SHAs: **17**
- SHAs with more than one run: **0**
- SHAs run under BOTH `push` and `pull_request` (CI-PRINCIPLE-06 anti-pattern): **0**

No head SHA has more than one run in the analysed window.
