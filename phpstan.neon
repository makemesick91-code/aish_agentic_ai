includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6

    paths:
        - app
        - bootstrap/app.php
        - config
        - routes

    tmpDir: storage/framework/cache/phpstan

    # Foundation-only runtime. As modules land, tighten level and widen paths.
