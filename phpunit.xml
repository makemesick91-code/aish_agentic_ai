<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Architecture">
            <directory>tests/Architecture</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- force="true" keeps the fast suite hermetic: it always uses sqlite/array/sync
             even when ambient env vars (e.g. the CI backend job's DB/REDIS settings) are
             present. Real PostgreSQL + Redis integration is proven by
             scripts/runtime/verify-runtime.sh, not this suite. -->
        <env name="APP_ENV" value="testing" force="true"/>
        <!-- Deterministic throwaway key for the test env only. NOT a secret; never used
             outside APP_ENV=testing. Real keys are per-environment and never committed
             (rule 04, rule 24). -->
        <env name="APP_KEY" value="base64:GhqcSC7YB7S0rVcTVWFh5Dx0jHKZOghZn98fV239yoc=" force="true"/>
        <env name="APP_TIMEZONE" value="Asia/Makassar"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="sqlite" force="true"/>
        <env name="DB_DATABASE" value=":memory:" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
